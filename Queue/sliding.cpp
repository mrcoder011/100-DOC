#include <iostream>
#include <dequeue>
using namespace std; 
int main (){
void sliding(int *arr , int n ){
    deque<int > dq;
    // base case 
    for (int i=0; i<k; i++){
        int element = arr[i];
       if(element < 0){
dq.push_back(i);

       }
        
    }
    // process remaing windows -> removal and addition ;
    for (int i=k; i<n; i++){
        cout << dq.front()<< endl;
        
        
    }
    if (n<=k){
        return;

    }
}

}