#include <iostream>
using namespace std;

/*int x = 2;  // Global variable

void fun() {
    int x = 60;          // Local variable inside fun()
    cout << x << endl;
    :: x = 40 ;   // Prints local x (60)
    cout << ::x << endl;  // Accesses global x using scope resolution (::)
}

int main() { 
    ::x = 4;             // Modifying the global x to 4
    int x = 20;          // Local variable inside main()

    cout << x << endl;    // Prints local x (20)
    cout << ::x << endl;  // Prints modified global x (4)

    fun();                // Calling the fun() function

    return 0;             // Properly placed return statement
}
 // so in this we learned that most local will be printed first then global 

*/
 //class
 class student{
    /*int id;
    int age;
    string name ;
    int nos;

    void study(){
        cout<< "studying"<< endl;

    }*/
 } ;
 int main (){
    cout<< sizeof(student)<< endl;

 }